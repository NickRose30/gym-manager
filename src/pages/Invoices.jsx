import React from 'react';
import styled from 'styled-components';

const Container = styled.div``;

const Invoices = () => (
  <Container>This is the invoices page</Container>
);

export default Invoices;